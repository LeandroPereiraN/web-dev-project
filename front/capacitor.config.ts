import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.superservice.app',
  appName: 'super-service',
  webDir: 'dist/superService/browser/',
};

export default config;
